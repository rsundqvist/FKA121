double x(vec3 *vec);
double y(vec3 *vec);
double z(vec3 *vec);

void x(vec3 *vec, double value);
void y(vec3 *vec, double value);
void z(vec3 *vec, double value);

double length(vec3 *vec);

vec3* copy(vec3 *vec);
vec3* norm(vec3 *vec);

double dot(vec3 *vec1, vec3 *vec2);
